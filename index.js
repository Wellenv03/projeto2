console.log ("jodador numero 1")

let.nikiname ("pastel")

const mensagem = "jogador numero 1"+ let.nikiname

let.saldoDeVitorias

//declarando operadores
let.numero = "0, 10"
console.log (saldoDeVitorias + numero)
console.log (ferro)

 calcular_ranked (vitorias, derrotas)
    saldo_vitorias = vitorias - derrotas

    ifsaldoDeVitorias < 10 (nivel) + "Ferro"

    return saldo_vitorias, nivel


vitorias = int(input("7: "))
derrotas = int(input("3: "))

saldo, nivel = calcular_ranked(vitorias, derrotas)

print.log("O Herói tem um saldo de {7 pontos} e está no nível de {ferro}")
